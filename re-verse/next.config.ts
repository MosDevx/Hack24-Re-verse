import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["assets.aceternity.com", "aceternity.com", "images.unsplash.com","picsum.photos"],
  }
  /* config options here */
};

export default nextConfig;
