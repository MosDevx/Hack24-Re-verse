import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["assets.aceternity.com", "aceternity.com", "images.unsplash.com","random-image-pepebigotes.vercel.app"],
  }
  /* config options here */
};

export default nextConfig;
